# mitumori — 曲げ加工見積もりシステム

高橋鉄骨の曲げ加工（板金曲げ）見積もりを計算するプロジェクト。GitHub Pages で公開している。

- トップ: https://jinu-h420.github.io/mitumori/
- 主力は **曲げ見積り計算書.html**（曲げ工賃のみ・単一HTML・約3,400行）。ロジック・価格表・得意先リストはすべてこのファイル内に埋め込み。

## 触る前に必ず読むもの

| 触る対象 | 必読ファイル |
|:---|:---|
| 曲げ見積り計算書.html | **曲げ見積り計算書_実装メモ.md**（現行仕様・localStorageキー・変更時の注意） |
| 見積り計算書.html（材料費込み） | **見積り計算書_仕様まとめ.md** |
| プロジェクトの方向性 | 見積書を育てる方針.md / 振り返りと改善方針_2026-07.md |

**曲げ見積り計算書.html と 見積り計算書.html は別ファイル・別ロジック**。曲げの単価表定義も異なる。どちらを変更しているか常に区別すること。

## データの正本（重要）

- **曲げ見積り計算書.html の価格は HTML 内の定数（BENDING_TABLE / SHIMA_* / KOMONO_DEFAULT_TABLE）が正本**。画面上のセル編集値は localStorage（端末ごと）に保存されるだけなので、価格を正式に変えるときは HTML 内の定数を変えること。
- `data/*.json`（prices / rates / hole_prices など）と `evals/` は**旧ロジック（基準価格×形状乗率×数量調整）用**。曲げ見積り計算書.html の現行ロジックとは別物で、evals が通っても現行HTMLの検証にはならない。

## 作業ルール

- コミットメッセージは日本語で「対象：変更内容」の形式（例: `曲げ見積り計算書：ハゼ折オプションを追加`）。
- **ファイル名の文字コード注意**: 日本語ファイル名は NFC（濁点合成済み）で統一。Mac/Googleドライブ経由で NFD 名が混入した事故歴あり（2026-07 に整理済み）。`git status` に「削除＋未追跡」のペアが並んだら NFD 混入を疑うこと。Mac 側では `git config core.precomposeunicode true` を推奨。
- 古くなった資料は `docs/archive/` へ移動する（削除しない）。

## 動作確認

ブラウザで 曲げ見積り計算書.html を直接開き、次を確認する:
1. 寸法入力 → 重量・曲げ長さの自動計算
2. 通常モードの表引き（形状・重量帯・長さ区分）とオプション加算
3. 小物モード（重量≤5kg かつ 長さ≤835mm で「小物に該当」→チェックONで切替）
4. F1記録 / F2リセット / F3元に戻す
5. 価格表セル編集 → リロード後も保持（localStorage）
