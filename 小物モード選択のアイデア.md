# 小物モードを「選択できる」ようにするアイデア

現在は **重量≤5kg かつ 曲げ長さ≤835mm** のとき自動で小物扱いになります。  
「自動ではなく、ユーザーが選べる」ようにするための案です。

---

## 方針の候補

### 案A: 3択ラジオ（おすすめ）

入力エリアに **「小物モード」** の選択を追加する。

| 選択肢 | 意味 |
|--------|------|
| **自動** | 今まで通り。寸法で判定（重量≤5kg かつ 曲げ長さ≤835mm → 小物） |
| **小物で計算** | 寸法に関係なく、曲げ回数×数量係数で計算する |
| **通常で計算** | 寸法に関係なく、形状の重量×長さの表引きで計算する |

- **置き場所**: 「入力」カード内、`<details>計算方法・小物判定について</details>` の上か横。または「形状」のすぐ下の行。
- **初期値**: 「自動」。ページ読み込み時は従来どおり。
- **保存**: 実績・ショートカット保存時にこの選択も保存し、読み込み時に復元する（既存の `inp.komonoMode` を「計算結果」として使っているので、**ユーザー指定**用に `komonoOverride: 'auto' | 'komono' | 'normal'` のようなキーを追加する）。

**メリット**: 意味がはっきりしていて、自動のままでも従来どおり動く。

---

### 案B: チェックボックス1つ「小物モードにする」

- **チェックON**: 常に小物ロジックで計算（寸法は見ない）。
- **チェックOFF**: 常に通常ロジック（表引き）。自動判定は使わない。

「自動」がなくなるので、「寸法的には小物だが表引きで出したい」も「寸法的には中物だが小物単価で出したい」も、どちらもユーザーがチェックで切り替える形になります。

**メリット**: 実装が簡単。  
**デメリット**: 「自動（寸法で判定）」が選べない。

---

### 案C: バナーをクリックでトグル

現在の「⚠ 小物モード」バナーを **クリック可能** にする。

- 自動判定で小物のとき: バナー表示。クリックで「このときは通常で計算」に切り替え。
- 自動判定で通常のとき: バナーは非表示のまま、どこかに「小物で計算する」ボタンやリンクを置き、押すと小物モードになる。

**メリット**: UIの追加が少ない。  
**デメリット**: 「今が自動か・手動か」が分かりにくくなりがち。

---

## 実装の共通ポイント（案Aを採用する場合）

1. **判定ロジックの変更（`calc()` 内）**

   ```js
   // 現在
   var isKomono = isKomonoByDimensions(wt, len);

   // 変更案: ユーザー指定を優先
   var override = getKomonoOverride(); // 'auto' | 'komono' | 'normal'
   var isKomono = (override === 'komono') || (override === 'auto' && isKomonoByDimensions(wt, len));
   ```

2. **`getKomonoOverride()`**  
   ラジオの `value` または `data-*` を読んで `'auto'` / `'komono'` / `'normal'` を返す。未実装時は `'auto'` として従来どおり。

3. **`syncKomonoBendUI`**  
   今は `isKomonoByDimensions(wt, len)` が false だと曲げ回数UIを隠している。  
   → **実際に小物で計算するか**（上の `isKomono`）で表示するか決めるように変更する（手動で「小物で計算」を選んだときも曲げ回数を表示する）。

4. **バナーの表示**  
   `toggleKomonoWrap(isKomono)` はそのまま、「小物で計算」が選ばれているか、自動判定で小物になったときに出せばよい。  
   必要ならバナーに「自動」/「手動」の区別を短いラベルで出してもよい。

5. **保存・復元**  
   - 実績やショートカット用の `inp` に `komonoOverride: 'auto'|'komono'|'normal'` を追加。
   - 読み込み時にラジオを `inp.komonoOverride` に合わせて設定し、`calc()` を実行。

6. **details の説明文**  
   「重量≤5kg かつ 曲げ長さ≤835mm のとき**自動で**小物扱い」の部分を、  
   「上の『小物モード』で**自動**を選んでいる場合、〜のとき小物扱いになります。」のように少しだけ文言更新すると分かりやすいです。

---

## まとめ

- **おすすめ**: **案A（3択: 自動 / 小物で計算 / 通常で計算）** を入力エリアに追加し、`calc()` では「ユーザー指定があればそれ優先、なければ寸法で自動判定」にする。
- 実装量を抑えたいなら **案B（チェック1つで小物ON/OFF）** でもよいが、その場合は「自動」はなくなります。
- 既存の「小物対応」バッジやバナーは、**今が小物で計算されているか**を示す表示として残し、案Aのラジオは「どう決めるか（自動か手動か）」の操作用に分けておくと整理しやすいです。

必要なら、案Aに沿った具体的なHTML/JSの差分（どこに何を追加するか）も書けます。
