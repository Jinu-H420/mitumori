# 曲げ見積り計算書（曲げ工賃のみ）— 実装メモ・Claude Code 用

**Claude Code で曲げ見積り計算書を触るときは、このファイルを読み込むこと。**

最終更新: 2026-07-06（コミット e4b1889 時点の実装に合わせて全面更新）

---

## 1. ファイルと役割

| ファイル | 役割 |
|:---|:---|
| **曲げ見積り計算書.html** | 曲げ工賃だけを検算する単体の計算書（約3,400行）。材料費・穴あけ・切断費は含まない。寸法入力で重量・長さを自動計算し、形状・数量・オプションで曲げ工賃を出す。実績の記録・一覧・GAS同期もこのファイル内。 |
| **実績一覧.html** | 実績レコードの一覧表示。 |
| **api/records.php** | 実績レコード用API（サーバ設置用）。現在の同期の主役はGAS（下記6章）。 |
| **index.html** | トップページ。「曲げ見積り計算書を開く」→ 曲げ見積り計算書.html、「見積り計算書を開く」→ 見積り計算書.html（材料費込みの本見積） |

- **曲げ見積り計算書** = 曲げだけ・検算用。あとで材料費見積りと合算する想定。
- **見積り計算書.html** = 材料費＋切断＋曲げ＋穴あけの全体（別仕様・別ロジック。見積り計算書_仕様まとめ.md 参照）。

---

## 2. 画面構成

- **寸法カード**: 材質（**通常鋼板／CHP**）・板厚（select）・縦L・横W・余長 → 重量・長さを自動計算（鉄 7.85）。
- **入力カード**: 形状（L曲げ／Z／コの字／C型／三方／四方／ハット／**複数曲げ**）、曲げ長さ（**縦L/横Wのどちらを使うか選択**＋直接入力）、数量、オプション、★記憶ボタン。
  - 複数曲げを選ぶと**曲げ回数（1〜8回）**を選択でき、表引きは C型曲げ にマッピング（shapeMap）。
  - 形状バッジに「曲げ回数／基準価格」を表示。
- **曲げ工賃内訳**: 基準価格 → 数量係数 → 数量補正後 → オプション加算 → 合計。
- **選択中の形状の表**: 重量帯×長さ区分の基準価格を一覧表示。**セル編集可能**（localStorage 保存）。
- **数量係数の表**: 閾値・係数とも**編集可能**（デフォルト: 1〜4個×1.5、5〜19個×1.0、20個〜×0.8）。
- **実績一覧セクション**: 記録した実績の一覧・編集・フィルター（お客様名）・重量列・全件再計算ボタン。

## 3. 板厚（select）

選択肢（mm）: —, 0.5, 0.6, 0.8, 1, 1.2, 1.6, 2, 2.3, 3, 3.2, 4, 4.5, 5, 6, 8, 9, 10, 12, 16。
未選択のときは寸法から重量を計算しない（直接入力の重量・長さを使う）。

---

## 4. 価格表と長さ区分

- **LENGTH_THRESHOLDS**: `[835, 1670, 2505, 3048, 99999]` → 列は 〜835mm, 〜1670mm, 〜2505mm, 〜3048mm, 3048mm超（**計5列**）。
  ※旧仕様にあった「〜500mm」列は**廃止**（2026-03頃）。
- **BENDING_TABLE**: 各行は `{ shape, w, c }`。`w` は重量上限(kg)（10, 20, 30, 50, 80, 100, 150, 9999）。`c` は長さ5区分の単価配列。
- 形状は7種（L曲げ、コの字曲げ、Z曲げ、C型曲げ、ハット曲げ、三方曲げ、四方曲げ）。曲げ回数の既定は **SHAPE_BEND_COUNT** = L:1 / Z:2 / コの字:2 / C型:4 / 三方:3 / 四方:4 / ハット:4。
- **CHP（縞板）**: 材質で CHP を選ぶと**専用テーブル（SHIMA_*）**を使う。形状区分も専用（L曲げ（短/長）、コ/Z（両端短/片側混在）、コの字（長）など）。こちらも編集・localStorage 保存あり。

---

## 5. 計算ロジック（要約）

### 小物モード
- **重量≤5kg かつ 曲げ長さ≤835mm** のとき「小物に該当」の行が現れる。**自動では切り替わらない** — ユーザーが「**小物モードで計算**」チェックボックス（`opt-komono-mode`）をONにしたときだけ小物モードになる（判定はチェックボックス状態に統一済み）。
- 計算: **曲げ回数**（1〜8回、デフォルト4回）ごとの基準単価 **KOMONO_DEFAULT_TABLE**（1回=400円、2回=500、3回=600、4回=800、5回=1000、6回=1200、7回=1500、8回=1800。**編集可・localStorage保存**）に、**数量係数**（「数量係数を反映する」チェックON時のみ）をかける。1個あたり**最低300円**。
- **小物専用オプション**: 中押し（×1.5）、逆曲げ（×1.2）、ハゼ折（+2,000円）。いずれも ×/+ の切替と数値編集が可能。
- **曲げ工賃の直接上書き**: 小物モード時は計算値を手入力で上書きできる欄あり（`komono-total-input`）。
- 小物モード中、通常のオプション欄は非表示。

### 通常モード
1. **基準価格**: 形状・重量・長さで **currentBendingTable** から表引き。重量は「入力重量以上の最小 w」の行、長さは「入力長さ以下の最小しきい値」の列。
2. **数量係数**: 「数量係数を反映する」チェック（`opt-use-qty-factor`、**デフォルトOFF・★記憶の対象外**）がONのときのみ適用。係数テーブル自体も編集可。
3. **オプション**: 中押し×1.5、逆曲げ×1.2、長尺目押し+2,500円、**ブロック**+3,000円（旧称「深曲げ」）、**ハゼ折**+2,000円。いずれも ×/+ 切替と数値編集が可能。
- ※旧仕様の「小物割引×0.8」は**廃止**。

---

## 6. ★記憶・ファンクションキー・実績同期

- **★記憶（オプション）**: オプションの数値・ON/OFF を記憶（`bending-estimate-input-memory`）。F2リセット後も復元。ただし「数量係数を反映する」は**記憶しない**（常にOFFで起動）。
- **★記憶（数量係数）**: 係数テーブルを記憶（`bending-estimate-qf-memory`）。
- **F1** = 実績を記録 / **F2** = リセット（実績一覧の絞り込みもクリア） / **F3** = 元に戻す（材質・板厚・形状も復元）。
- **実績のGAS同期**: `GAS_API_URL`（Google Apps Script）へ自動同期。**タブ復帰時に自動同期**して再計算は安全化済み。
- **登録ブラウザの識別**: 初回に採番したブラウザID（`bending-browser-id`）を記録に付与し、「自分の記録かどうか」を番号で自動識別。
- **得意先**: 得意先No→社名のリストをHTMLに内蔵（FALLBACK_CUSTOMER_CSV）。**得意先一覧モーダル内のCSV読み込み**で差し替え可（`bending-customer-list-csv`）。得意先No検索・検索履歴（`bending-customer-search-history`）あり。

---

## 7. localStorage キー一覧

| キー | 内容 |
|:---|:---|
| `bending-estimate-price-table` | 通常の基準価格表（編集値） |
| `bending-estimate-shima-price-table` | CHP（縞板）価格表（編集値） |
| `bending-estimate-komono-table` | 小物の曲げ回数→基準単価（編集値） |
| `bending-estimate-records` | 実績レコード |
| `bending-browser-id` | このブラウザの登録番号 |
| `bending-customer-list-csv` | 得意先リストCSV |
| `bending-customer-search-history` | 得意先検索履歴 |
| `bending-estimate-input-memory` | ★記憶（オプション） |
| `bending-estimate-qf-memory` | ★記憶（数量係数） |
| `bending_load_request` | 実績一覧→計算書への読み込み要求 |

**注意**: 価格表の編集値は**ブラウザ・端末ごと**に保存される。端末間で見積り額がズレる原因になるので、価格を正式に変えたら HTML 内のデフォルト（BENDING_TABLE 等）にも反映すること。

---

## 8. 変更時の注意（Claude Code で触る場合）

- **BENDING_TABLE** の行数・形状名・`w` の並びを変えると、**loadBendingTable** の検証で不一致になり、localStorage は使われず BENDING_TABLE のコピーで初期化される。列数（`c.length`）も LENGTH_THRESHOLDS.length（現在5）と一致させること。KOMONO・SHIMA テーブルも同様に検証つき。
- 曲げ見積り計算書.html は **見積り計算書.html** とは別ファイル・別ロジック。どちらを触っているか必ず区別すること。
- index.html のリンク先は相対パス。同じフォルダにないと開けない。
- **ファイル名の文字コード**: このリポジトリは過去にMac/Googleドライブ経由で濁点分解（NFD）名が混入し、2026-07に整理済み。Macで作業した後は `git status` に「削除＋未追跡」のペアが出ていないか確認すること（Mac側は `git config core.precomposeunicode true` を推奨）。

---

## 9. 関連ドキュメント

- **CLAUDE.md** — リポジトリ全体の入口（必読ファイル・正本データ・注意点）。
- **見積り計算書_仕様まとめ.md** — 材料費込みの「見積り計算書.html」の仕様。
- **振り返りと改善方針_2026-07.md** — プロジェクト棚卸しと改善の優先順位。
- **小物モード選択のアイデア.md** — 小物モードUIの検討経緯。
- **計算書の引き渡し説明書.md** — 利用者向けの説明。
- **進捗確認_曲げ見積り.md** — evals と見積り計算書.html の進捗（evals は旧ロジック用。曲げ見積り計算書.html 単体の evals は未整備）。
